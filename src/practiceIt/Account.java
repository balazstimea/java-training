package practiceIt;

public class Account {
/*    public boolean __processCalled;

    public Account(Client c) {
        __processCalled = false;
    }

    public boolean process(Transaction t) {
        __processCalled = true;
        return t.value() > -100 && t.value() < 1000000;
    }
}
class Client {}

class Transaction {
        private int value;

        public Transaction(int v) {
            value = v;
        }

        public int value() {
            return value;
        }

    }
class FilteredAccount extends Account {
    private int totalTransactions;
    private int filterdTransactions;
    public FilteredAccount(Client c){
        super(c);
    }

    public boolean process(Transaction t) {
        __processCalled = true;
        return t.value() > 0 && t.value() < 1000000;
    }



}

class MainOfAccount {
    public static void main (String[] args) {
        Account account= new Account(new Client());
        FilteredAccount filteredAccount= new FilteredAccount(new Client());
    }
    */
}
